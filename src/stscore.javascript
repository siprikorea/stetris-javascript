class CStScore
{
    // Constructor
    constructor();

    // Clear
    Clear();

	// Add score
	AddScore(unsigned int dwScore);
	// Set score
	SetScore(unsigned int dwScore);
	// Get score
	GetScore();

	// Score
    m_dwScore;
};
